def random_forest():
    """
    Random Forest is an ensemble of Decision Trees.
    It uses Bootstrap Aggregation (Bagging).
    """
    print("Random Forest implementation relies on building multiple Decision Trees.")
    print("Steps:")
    print("1. Create N bootstrap samples.")
    print("2. Train a decision tree on each sample.")
    print("3. For prediction, aggregate predictions from all trees (Majority Voting).")

if __name__ == "__main__":
    random_forest()
